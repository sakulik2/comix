# 复制成 keystore.properties 后填入真实值。
# 该文件已被 .gitignore 挡住，密码不会进版本库。
# storeFile 相对仓库根目录，也可写绝对路径。
# 注意 .properties 把反斜杠当转义符，Windows 路径要写成 C:/Users/me/key.jks
# 或 C:\\Users\\me\\key.jks，否则分隔符会被吃掉。
storeFile=comix-release.jks
storePassword=
keyAlias=
keyPassword=

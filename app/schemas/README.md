# Room Schemas

Android Studio builds export Room schema snapshots to this directory. Commit each new version's JSON file together with its migration so future upgrades can be validated without destructive fallback behavior.
